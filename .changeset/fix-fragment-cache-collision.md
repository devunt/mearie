---
"@mearie/core": patch
---

Fix fragment cache collision where different fragments for the same entity overwrote each other's data when selecting the same nested object field with different sub-fields
