---
"@mearie/svelte": patch
"@mearie/react": patch
"@mearie/vue": patch
"@mearie/solid": patch
---

Fix `skip` option not setting `loading` to `false` when activated, and `refetch()` not executing when `skip` is `true`
